---
# Pinned for supply-chain hygiene. Bump deliberately, with `uv run pytest`
# verifying the playbook still passes syntax + lint after upgrades.
collections:
  - name: ansible.windows
    version: "3.5.0"
  - name: community.windows
    version: "3.1.0"
